import OTPVerificationSkeleton from "@/modules/auth/ui/components/otp-skeleton";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <OTPVerificationSkeleton />;
}
